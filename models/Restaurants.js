const mongoose = require("mongoose");
const Schema = mongoose.Schema;
const thingsSchema = new Schema({})
module.exports = Restaurants = mongoose.model("Restaurants",thingsSchema,"restaurants_collection");